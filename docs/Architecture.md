# Architecture

Purpose: High-level map of the project's three strict layers (Presentation / Application / Domain), directory ownership, and how data flows between Next.js → Zustand → Socket.IO → Game Engine. Intended as the first read for any agent joining the project.
