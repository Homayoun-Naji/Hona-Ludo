# Board Specification

Purpose: Defines the SVG-based 5-player star-shaped board geometry, tile coordinates, per-color paths, and home stretches. Consumed by `config/board.config.ts` once the board is implemented. Intended to be the single reference for engine authors and SVG renderers.
