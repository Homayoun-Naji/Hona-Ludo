# Socket Events

Purpose: Living inventory of every real-time event exchanged between client and server, including direction (client→server / server→client), payload shape, and expected side effects. Kept in sync with `constants/events.ts`.
