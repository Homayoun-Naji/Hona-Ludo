/**
 * Telegram Mini App SDK core integration.
 *
 * Phase 4 wires the `@twa-dev/sdk` (or `@telegram/web-app`) init and the
 * `WebApp` entry point. Until then this module only reserves the namespace.
 */
