/**
 * Telegram Mini App SDK configuration (bot username, base URL, feature
 * flags such as the BackButton or theme support).
 *
 * Implemented in Phase 4 (Telegram Mini App Integration). No API calls
 * are made here; this only reserves the configuration module.
 */
export type TelegramConfig = {
  /** Reserved for SDK init settings. */
  readonly placeholder: true;
};
