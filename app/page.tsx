/**
 * Home route.
 *
 * Phase 3 replaces this with the real game lobby. For now this is
 * the single Next.js route so `next dev` has a page to serve.
 */
export default function Home() {
  return (
    <div>
      منچ هُنا — بازی لودو ۵ نفره
    </div>
  );
}
